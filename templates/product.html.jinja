<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href= "/static/styles.css" rel="stylesheet" >
  </head>
  <body>
  {% include "components/navbar.html.jinja"%}
    
    <div class="grid grid-cols-2 ml-20 gap-15 mr-20">
            <img class="h-175 w-300 border-black border-[10px] justify-center fill" src=" {{ product ['Image'] }}">
            <div class="text-xl">{{ product ["Name"] }}<div>
            <br>
            <div>${{ product ["Price"] }}</div>
            <br>
            <div class="text">{{ product ["Size"]}}</div>
            <br>
          <div class="flex flex-co text-wrap">
            <div> Desctiption: {{ product ["Description"]}}</div>
          </div>
    </div>
           <br>
           <br>
           
           <form method ="post" action="/product/{{ product['ID']}}/add_to_cart">
           <label>Qty:</label>
           <input name="qty" type="number" value="1" min="1" max="10">
            <button class="text-white text-deco px-25 py-2 rounded-lg bg-blue-700 hover:text-white justify-center ml-3" type="submit">
                <i data-lucide="send"></i> Add to cart
            </button>
            </form>

  </body>
</html>
